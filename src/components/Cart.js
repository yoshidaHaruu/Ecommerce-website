import React from 'react';

const Cart = () => {
  return (
    <div className="cart">
      <h2>Cart</h2>
      {/* Render the items in the cart */}
    </div>
  );
};

export default Cart;
